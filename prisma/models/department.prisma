model Department {
  id          String     @id @default(cuid())
  name        String     @unique
  
  stockOuts   StockOut[] 

  printers  Printer[]

  movementsOut    PrinterMovement[] @relation("FromDepartment")
  movementsIn     PrinterMovement[] @relation("ToDepartment")
}